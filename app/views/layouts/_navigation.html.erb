<nav class="navbar navbar-expand-lg bg-primary sticky-top">
  <div class="container-fluid">
    <%= link_to "Alpha Blog", root_path, class: "navbar-brand link-light" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Bloggers", users_path, class: "nav-link link-light" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link link-light dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Articles
          </a>
          <ul class="dropdown-menu">
            <% if logged_in? %>
              <li>
                <%= link_to "Create Article", new_article_path, class: "dropdown-item" %>  
              </li>
            <% end %>
            <li>
              <%= link_to "View Articles", articles_path, class: "dropdown-item" %>  
            </li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link link-light dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Categories
          </a>
          <ul class="dropdown-menu">
            <% if logged_in? && current_user.admin? %>
              <li>
                <%= link_to "Create Category", new_category_path, class: "dropdown-item" %>  
              </li>
            <% end %>
            <li>
              <%= link_to "View Categories", categories_path, class: "dropdown-item" %>  
            </li>
          </ul>
        </li>
      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
      </form>
      <ul class="navbar-nav">
        <% if logged_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link link-light dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.username %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li>
                <%= link_to "View Profile", user_path(current_user), class: "dropdown-item" %>  
              </li>
              <li>
                <%= link_to "Edit Profile", edit_user_path(current_user), class: "dropdown-item" %>  
              </li>
              <li>
                <%= link_to "Delete Account",
                    user_path(current_user),
                    data: {
                      turbo_method: :delete,
                      turbo_confirm: "Are you sure you want to delete your account?"
                    },
                    class: "dropdown-item text-danger" %>  
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= link_to "Logout", logout_path, class: "dropdown-item", data: { turbo_method: :delete } %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", login_path, class: "nav-link link-light" %>  
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", signup_path, class: "nav-link link-light" %>  
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
